# ZCU208 CLK104 clocks (LMK04828 + dual LMX2594) sit behind an NXP SC18IS602B
# I2C-to-SPI bridge (see edf_bsp/board.dtsi); this driver lets them enumerate
# under /sys/bus/spi/devices so xrfclk can program them.
CONFIG_SPI_SC18IS602=y
